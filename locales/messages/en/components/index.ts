import { welcomeDialog } from "./welcome-dialog";
import { videoUploader } from "./video-uploader";
import { common, logo, dropzoneArea } from "./common";

export const components = {
  welcomeDialog,
  videoUploader,
  common,
  logo,
  dropzoneArea,
} as const;
